import { RouteSkeleton } from "@/components/layout/route-skeleton"

export default function SettingsPage() {
  return <RouteSkeleton title="Settings" description="Settings root for Profile, Team, Notification and Integration." screenId="SET-ROOT" />
}
