import { RouteSkeleton } from "@/components/layout/route-skeleton"

export default function ProfilePage() {
  return <RouteSkeleton title="Profile" description="Profile settings skeleton for account identity and preferences." screenId="SET-PROFILE" />
}
