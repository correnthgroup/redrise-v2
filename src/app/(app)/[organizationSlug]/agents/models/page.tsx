import { RouteSkeleton } from "@/components/layout/route-skeleton"

export default function ModelsPage() {
  return <RouteSkeleton title="Models" description="Model registry foundation. Provider and model setup comes in a later PRD." screenId="AG-MODELS" />
}
